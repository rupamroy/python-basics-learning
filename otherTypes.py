complex

# long  # Only in Python 2

bytes  # sequence of integers in the range of 0 - 255, seq of strings , other objects etc

bytearray  # similar to bytes

tuple = (3, 5, 1, "Mark")  # similar to list but are immutable

set  # similar to tuple but only has unique objects

frozenset  # similar to tuple but only has unique objects

set([3, 2, 3, 1, 5]) == (1, 2, 3, 5)
